[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'cd server && bunx prisma migrate deploy && bun src/index.ts'"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
