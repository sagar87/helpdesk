{
  "name": "helpdesk",
  "private": true,
  "workspaces": ["core", "client", "server"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:client": "bun run --filter client dev",
    "dev:server": "bun run --filter server dev",
    "build": "bun run --filter client build && cd server && bunx prisma generate",
    "start": "cd server && bun src/index.ts",
    "db:migrate": "cd server && bunx prisma migrate deploy",
    "test": "bun run --filter client test",
    "test:watch": "bun run --filter client test:watch",
    "test:e2e": "bun e2e/setup-db.ts && bunx playwright test",
    "test:e2e:ui": "bun e2e/setup-db.ts && bunx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/pg": "^8.20.0",
    "pg": "^8.20.0"
  }
}
